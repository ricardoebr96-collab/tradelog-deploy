[build]
  # Sitio estático: no hay paso de build, se publica la raíz del repo.
  publish = "."
  command = ""

[[redirects]]
  # Cualquier ruta sirve la app (SPA de una sola página).
  from = "/*"
  to = "/index.html"
  status = 200
